-- ============================================================================
-- 给进度证书表加「完整证书内容」列（cert jsonb）
-- ----------------------------------------------------------------------------
-- Supabase → SQL Editor → New query → 粘贴 → Run。跑一次即可，可重复跑。
-- 一张证书有很多栏位（项目信息、分包商信息、计算表金额、签名名字），
-- 全部存进这个 jsonb 里，方便一次读写、原样列印。
-- 需先跑过 add-certificates.sql（建 subcon_claims 表）。
-- ============================================================================

alter table public.subcon_claims
  add column if not exists cert jsonb;
