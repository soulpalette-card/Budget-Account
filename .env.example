# ===== 大白话说明 =====
# 这是「环境变量样板文件」。真正用的时候：
#   1. 把这个文件复制一份，改名叫  .env
#   2. 把下面两个值换成你自己 Supabase 项目里的真值
#   3. .env 已经被 .gitignore 挡住了，不会上传，放心填
#
# 去哪里拿这两个值？
#   打开 Supabase 后台 → 你的项目 → 左下角 Settings（齿轮）→ API
#   - Project URL          就是下面的 VITE_SUPABASE_URL
#   - Project API keys 里的 "anon public"  就是下面的 VITE_SUPABASE_ANON_KEY
#
# ⚠️ 千万注意：只用 "anon public" 这把钥匙！
#    绝对【不要】把 "service_role" 那把钥匙填进来——它会绕过所有安全规则(RLS)，
#    放到前端等于把整个数据库大门敞开给所有人。

VITE_SUPABASE_URL=https://你的项目编号.supabase.co
VITE_SUPABASE_ANON_KEY=你的_anon_public_钥匙_一长串字母数字
